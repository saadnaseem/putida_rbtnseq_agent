# Core — the API needs only these.
pandas>=2.0
pyarrow>=12.0

# build_cache.py reads the paper supplement (.xlsx)
openpyxl>=3.1

# plot_gene_profile / plot_substrate_heatmap
matplotlib>=3.7

# tests
pytest>=7.0
